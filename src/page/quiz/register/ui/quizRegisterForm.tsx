import PrimaryButton from "../../../../ui/button/primaryButton.tsx";
import QuizForm from "../../ui/quizForm.tsx";

function QuizRegisterForm() {
    return (
        <QuizForm>
            <PrimaryButton color={"primary"}>등록</PrimaryButton>
        </QuizForm>

    );
}

export default QuizRegisterForm;
